server:
  port: 9091
spring:
  application:
    name: orderservice
eureka:
  client:
    serviceUrl:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka}
app:
  orderProcessName:
    name: ${ORDER_PROCESSOR_NAME:orderprocessor}